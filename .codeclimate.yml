plugins:
  phpcodesniffer:
    enabled: true
    config:
      standard: "PSR1,PSR2,PHPCS"
  phpmd:
    enabled: true
  phan:
    enabled: true
    config:
      file_extensions: "php"
  sonar-php:
    enabled: true
    config:
      tests_patterns:
        - SocketsServer/Tests/**
